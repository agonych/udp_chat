# Copy to: secret.yaml (DO NOT COMMIT the real one)
apiVersion: v1
kind: Secret
metadata:
  name: udpchat-app
type: Opaque
stringData:
  # ---- Database (prod) ----
  DB_HOST: "REPLACE_DB_HOST"
  DB_PORT: "5432"
  DB_NAME: "udpchat"
  DB_USER: "udpchat_user"
  DB_PASSWORD: "REPLACE_STRONG_PASSWORD"

  # Optional external API(s)
  OPENAI_API_KEY: ""

  # Connector → UDP relay (these may be omitted; values already set non-secret)
  CONNECTOR_UDP_HOST: "udp-server"
  CONNECTOR_UDP_PORT: "9999"
