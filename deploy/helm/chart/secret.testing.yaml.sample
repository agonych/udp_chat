# Copy to: secret.testing.yaml (DO NOT COMMIT the real one)
apiVersion: v1
kind: Secret
metadata:
  name: udpchat-app
type: Opaque
stringData:
  # Database connection (change these values as needed)
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "udpchat"
  DB_USER: "udpchat"
  DB_PASSWORD: "change_me_please" # Change this password for better security

  # OpenAI API key for GPT mode
  OPENAI_API_KEY: "" # Set this if you want to use GPT mode
